
import { AppBar, styled, Toolbar } from '@mui/material';

export const AppNav = styled(AppBar)({
    boxShadow: '0px 0px 16px rgba(77, 196, 136, 0.25)',
});


export const ToolApp = styled(Toolbar)({
    height: '80px',
    backgroundColor: 'white',
});